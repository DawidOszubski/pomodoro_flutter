import 'package:flutter/material.dart';

class FlashCardItemWidget extends StatelessWidget {
  const FlashCardItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
